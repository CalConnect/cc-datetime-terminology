image:
  name: ribose/metanorma
  entrypoint: [""]

cache:
  paths:

build:
  stage: build
  script:
    - bundle
    - make clean publish
    - mv publish public
  artifacts:
    paths:
      - public
